# Working directories (created at runtime, not in repo)
.cpcache/
.nrepl-port

# OS
.DS_Store

# Logs are local
missions/logs/*.log

# Runtime state (local, not code)
madeline/repos.edn
madeline/memory.edn
