# Working directories (created at runtime, not in repo)
.cpcache/
.nrepl-port

# Editor / LSP tooling caches
.clj-kondo/
.lsp/

# OS
.DS_Store

# Logs are local
missions/logs/*.log

# Runtime state (local, not code)
madeline/repos.edn
madeline/memory.edn
